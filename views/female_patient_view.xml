<?xml version="1.0" encoding="utf-8" ?>
<odoo>

    <record id="action_hospital_patient_female" model="ir.actions.act_window">
        <field name="name">Female Patients</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">hospital.patient</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{'default_gender': 'female', 'default_age': 25}</field>
        <field name="domain">['|','|',('gender','=','female'),('age','&lt;','10'),('gender','=','male')]</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                This is help of action, of placeholder
            </p>
        </field>
    </record>

        <menuitem id="menu_female_patient"
              name="Female Patient"
              action="action_hospital_patient_female"
              parent="menu_patient_submenu"
              sequence="0"
    />
</odoo>


<!--We can set default value of a field using context,

just before writeing a field in context write default_   it will consider as default next give the field name default_gender
<field  name="context">{'default_gender': 'female', ‘default_age’: 25}</field>
Note

just before writing a field in context write default_   it will consider as default next give the field name default_gender-->