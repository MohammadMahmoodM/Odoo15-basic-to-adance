<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="view_hospital_patient_tree" model="ir.ui.view">
        <field name="name">hospital.patient.tree</field>
        <field name="model">hospital.patient</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
                <field name="age"/>
                <field name="gender"/>
            </tree>
        </field>
    </record>

    <record id="view_hospital_patient_form" model="ir.ui.view">
        <field name="name">hospital.patient.form</field>
        <field name="model">hospital.patient</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="age"/>
                        <field name="gender"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>
    <!--In form view we can give namming convention, as model name so in name just define model name and form or tree etc, and same way id that is understandable-->
    <record id="every_action_and_Record_have_id" model="ir.actions.act_window">
        <field name="name">Patients</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">hospital.patient</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                This is help of action, of placeholder
            </p>
        </field>
    </record>
</odoo>


        <!--Its has a name, further we defined type of action, view mode are many but we select two, there postition matters, is tree is writen first, or form
        and in field of help its called, any helping words
        -->